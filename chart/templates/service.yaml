apiVersion: v1
kind: Service
metadata:
  name: {{ include "vector-controller.fullname" . }}
  labels:
    {{- include "vector-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vector-controller.selectorLabels" . | nindent 4 }}
