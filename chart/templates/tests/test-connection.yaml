apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vector-controller.fullname" . }}-test-connection"
  labels:
    {{- include "vector-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vector-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
